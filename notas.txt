Contexto:
- Prop drilling: passar a mesma propriedade vários niveis abaixo 
- Compartilhamento de estado vários componentes da aplicação
- Quando a informação não parece estar no local correto
- useContext

performace:


SSR(server-side-rendering): melhora a indexação com o google

scoped css aplicado apenas em um componente


stripe: api utilizado para pagamentos

ssg( static site generation):
cria um arquivo html estático